# shellprocess@finish-boot.conf
#
# Runs AFTER Calamares' bootloader module (systemd-boot). Patches loader
# entries with the computed kernel cmdline (Btrfs subvol, LUKS, resume).
---
dontChroot: false
timeout: 300

script:
  - "/usr/local/bin/ii-finish-systemd-boot"
