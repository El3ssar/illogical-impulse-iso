# hwclock.conf
# Set the hardware clock to UTC — the standard for Linux systems.
# Windows users dual-booting will need to set Windows to use UTC too,
# or use the RealTimeIsUniversal registry key.
---
utc: true
