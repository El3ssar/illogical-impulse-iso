# bootloader.conf — systemd-boot (UEFI only, no GRUB on installed system).
---
efiBootLoader: "systemd-boot"
efiBootLoaderId: "IllogicalImpulse"

installEFI: true

# Removable-media fallback: /EFI/BOOT/BOOTX64.EFI (VirtualBox, flaky NVRAM).
installEFIFallback: true

timeout: 5
