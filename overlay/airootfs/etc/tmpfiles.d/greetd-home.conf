# Re-create greetd's state directory on every boot. greeter's home in
# /etc/passwd is /var/lib/greetd; if this dir is missing greetd dies at
# startup with "unable to set working directory" and the box drops to
# agetty on tty1.
d /var/lib/greetd 0755 greeter greeter -
