# Tee greetd's stderr+stdout to a fixed log path so a user without
# journalctl experience can still diagnose graphical-session failures
# from the tty2 rescue shell.
[Service]
StandardError=append:/var/log/greetd-boot.log
StandardOutput=append:/var/log/greetd-boot.log
