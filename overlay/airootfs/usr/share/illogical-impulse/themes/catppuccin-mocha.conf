# Illogical Impulse flavor seed — Catppuccin Mocha.
#
# A flavor is a SEED fed through upstream's own Material You pipeline, NOT a
# frozen palette: `iictl theme set catppuccin-mocha` reads `seed` below and runs
# upstream `switchwall.sh --color <seed>`, so matugen harmonizes the whole rice
# around it (it is NOT all-or-nothing — that is the differentiator vs frozen
# colour sets). key=value, text-only, comment-tolerant. `seed` (a #RRGGBB hex)
# is mandatory; `label` and `mode` (dark|light) are optional metadata.
label=Catppuccin Mocha
seed=#89b4fa
mode=dark
