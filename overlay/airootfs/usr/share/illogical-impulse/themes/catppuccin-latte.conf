# Illogical Impulse flavor seed — Catppuccin Latte (light).
# The seed is fed through upstream switchwall.sh --color so matugen harmonizes
# the rice around it; `mode=light` is metadata for the GUI picker (out of scope
# here). See catppuccin-mocha.conf for the full format note.
label=Catppuccin Latte
seed=#1e66f5
mode=light
